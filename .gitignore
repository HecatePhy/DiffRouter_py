fpga24_routing_contest/
*.log
# Ignore all of data/ (device file, benchmarks, net indices — all large / external).
# data/* rather than data/ so git can still descend for the RRG exception at the bottom.
data/*
results/
docs/

__pycache__/
*.py[cod]
*$py.class
.Python
*.so
.env
.venv/
venv/

cpp/build/

# Large generated / local artifacts
*.pt
*.phys
*.dcp
*.device
*.png
campaign/
results_*/

# Keep the 12 MB RRG (small build artifact needed to run anything); must be LAST
# to override both data/* and *.pt above.
!data/rrg_xcvu3p_int.pt
